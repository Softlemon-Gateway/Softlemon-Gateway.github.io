{
  "name": "softlemon-api-docs",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "zudoku dev",
    "prebuild": "node scripts/check-placeholders.mjs && rm -rf public/specs && cp -R specs public/specs",
    "build": "zudoku build",
    "postbuild": "node scripts/strip-preconnect.mjs",
    "preview": "zudoku preview",
    "snippets": "node scripts/generate-snippets.mjs",
    "postman": "node scripts/generate-postman.mjs"
  },
  "dependencies": {
    "react": ">=19.2.0",
    "react-dom": ">=19.2.0",
    "zudoku": "0.83.0"
  },
  "devDependencies": {
    "@scalar/snippetz": "0.9.23",
    "openapi-to-postmanv2": "^6.3.2"
  }
}
